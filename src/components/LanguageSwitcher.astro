---
// src/components/LanguageSwitcher.astro
interface Props {
  lang: string;
}

const { lang } = Astro.props;
---

<div class="inline-flex rounded-md border border-gray-200 dark:border-gray-700 overflow-hidden">
  <button
    lang="sv"
    onclick="document.cookie='lang=sv;path=/;max-age=31536000';location.reload();"
    class:list={[
      'px-3 py-1 text-sm font-medium transition-colors',
      lang === 'sv'
        ? 'bg-blue-600 text-white'
        : 'bg-white text-gray-600 hover:bg-gray-50 dark:bg-gray-800 dark:text-gray-300 dark:hover:bg-gray-700',
    ]}
  >
    SV
  </button>
  <button
    lang="en"
    onclick="document.cookie='lang=en;path=/;max-age=31536000';location.reload();"
    class:list={[
      'px-3 py-1 text-sm font-medium transition-colors border-l border-gray-200 dark:border-gray-700',
      lang === 'en'
        ? 'bg-blue-600 text-white'
        : 'bg-white text-gray-600 hover:bg-gray-50 dark:bg-gray-800 dark:text-gray-300 dark:hover:bg-gray-700',
    ]}
  >
    EN
  </button>
</div>
